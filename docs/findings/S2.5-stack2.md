# S2.5 — rung-1 (stack2) hardening: built, verified, pilot PASSES the ceiling

**Date:** 2026-08-24
**Slice:** S2.5 (branch `s2.5-stack2`), ops record for spec
`docs/superpowers/specs/2026-08-23-crucible-s2.5-stack2-design.md` §8.
**Verdict:** **p0 = 0.267 (8/30) ≤ 0.70 ⇒ `too_easy = false` — hardening rung FIXED at
`stack2` (pre-reg §4.8.4; the ladder stops at rung (i)).** Rung 0 read 0.767 on the same
protocol; two-site mutants moved the ceiling by half a point.

## 1. What ran (chronological)

| Step | Result |
|---|---|
| Dry-run build (25 units, C=5) | `NotEnoughClasses: eligible 4 < C=5` — the post-walk guard refusing a short stream, loudly. 48 s |
| Full rung-1 build (attempts 1–3) | OOM-killed by the scope cap at ~15.3 min each (8G/12G/12G peaks). Root cause: default `--jobs 8`; the proven rung-0 recipe was `--jobs 6` (11G peak). NOT a tmpfs leak (that was cleaned and ruled out separately). |
| Full rung-1 build (attempt 4) | `--jobs 5`, `MemoryMax=12G`, disk `TMPDIR` → **streams/1158e92f40ad**, 450 tasks, 43 min, precheck ok inside the build |
| `crucible stream precheck` (record) | **ok=True, all 8 checks** incl. `distinct-sites` (site-sets) and `two-site-at-stack2` |
| `crucible stream smoke` 30 | **30/30 killed, 0 infra** |
| Rung-0 full-hash guard (spec §6) | Rebuild on the post-branch code reproduced **`dd5912cddedc`** exactly (content-addressed path taken). 26 min |
| §4.7 landing re-probe | 1.5B chat-served on the NEW stream: **rate 1.00 (30/30), passes** |
| **Ceiling re-pilot (§4.8.4 verbatim)** | A_noMem, 30 phase-1 tasks, K=8, pinned sampler, seed 0 → **`{"n": 30, "p0": 0.2667, "too_easy": false, "recommendation": "proceed"}`**, 6.7 min. Records: `runs/pilot-stack2-15bc/A_noMem/` |

## 2. The rung-1 stream

`streams/1158e92f40ad` — seed 0, C=200, n_nov=50, per_family=6, pairs_per_family=4, rung
`stack2`. Census: `eligible_classes 207` (C=200 cleared by 7 — the pre-build capacity
estimate of 238 non-CONST classes minus stacked-composite attrition), `classes_taken 200`,
`valid_mutants 1006` (valid stacked candidates), `stack-apply 347`, `unit-no-valid 213`,
`ineligible-class 390`, `equivalent 2`, `hidden-only 1`, `infra 0`, `syntax 0`,
`units-unused 9`.

- **CONST contributes zero rung-1 classes** (structural, ruled): `NumberReplacer` yields two
  mutation positions at the identical span per numeric literal, so span re-selection is
  always ambiguous and every CONST pair drops as `stack-apply` — the bulk of the 347 floor.
  The smoke sample shows the expected SDL/ARITH/BOOL/CMP/UNARY mix.
- **`stack-apply` census definition** (ruled at build): every `compose_pair` that returned
  `None` — apply failures and the shared guards (equivalent-to-a-single, non-compiling)
  alike. Composite equivalents that *applied* but pass the visible suite appear under
  `equivalent` (2 here).
- **Rung-1 counts are a census over stacked candidates only**; the singles' verdicts are on
  disk (provenance for R-S25-1's component half) but not census-counted. Not comparable
  1:1 with rung-0 counts.
- `spans_overlap` is inclusive-end (conservative vs the spec's half-open phrasing, ruled):
  boundary-touching pairs are rejected; the attrition is absorbed by the capacity margin.

## 3. Lens (for the pilot number)

`{success_def: hidden∪visible pass, module-only patch; budget: K=8 + 900s valve; sampler:
T=0.7, top_p=0.95, max_new_tokens=2048 (A1), thinking off, seed rule per driver; proposer:
Qwen/Qwen2.5-Coder-1.5B-Instruct CHAT-served (A2), identity-asserted; server: vLLM,
VLLM_USE_FLASHINFER_SAMPLER=0, port 8010, SERVE-table flags (2B-parity, confirmed live this
session); stream: 1158e92f40ad, rung stack2}`

## 4. Ops gotchas (recorded so nobody re-buys them)

- **`--jobs 8` OOMs the build at any cap tried (8G/12G); `--jobs 6` was the proven rung-0
  recipe (11G peak), `--jobs 5` used here.** The kill point is a deterministic memory burst
  in `build_units`, position-stable across caps. Build under
  `systemd-run --user --scope -p MemoryMax=12G -p MemorySwapMax=0` with `--jobs 5`.
- OOM-killed builds leak `/tmp/crucible-*` sandbox workdirs into tmpfs (RAM). Point the
  build's `TMPDIR` at a real-disk dir (`runs/tmp`) and clean after.
- `scripts/serve_model.sh` needs `.venv/bin` on PATH (bare `vllm` otherwise not found), and
  the SERVE table was missing the A2 proposer until commit `9586aa8` (2B-parity flags,
  confirmed live: bf16 1.5B serves clean at util 0.6).
- **One validation verdict is flaky at the hidden-only/equivalent boundary**: the rung-0
  guard rebuild reproduced the stream hash exactly but its census read `equivalent 541 /
  hidden-only 318` vs the original `540 / 319` — one borderline mutant flips between
  builds (both reasons invalid, task list unaffected, hash excludes counts by design).
  Known-class, count-level only; worth an eye at lock time.
- Pre-branch stream dirs fail the new `counts-named` precheck (no `stack-apply` key) until
  rebuilt/rewritten — `dd5912cddedc` was rewritten by the guard build and now passes. An
  old dir failing precheck before such a rewrite is stale serialization, not CONFOUNDED.

## 5. What this unblocks

The gating A_full-vs-A_noMem run (S4) now has a discriminating stream. Δ_min at lock
computes from p0=0.2667 and C=200. Next per the roadmap: 9B baseline §4.7 landing probe
(gates B_naive/big, not this pilot), then S3 (memory organ), then lock+run.
